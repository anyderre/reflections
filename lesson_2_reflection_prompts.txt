What happens when you initialize a repository? Why do you need to do it?
when you initialize a repository you declare that this the files from that taht directory will be manage by git. It is beca

How is the staging area different from the working directory and the repository?
What value do you think it offers?
R- The staging area is where the user add it's recent changes before commiting while in the working directory the user perfoms its changes.

How can you use the staging area to make sure you have one commit per logical
change?
by adding one element at a time with the command git add, you can be sure that you have one commit per logical change. You are free to control what file to commit first based on the idea of a logical change.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
branches are helpful when you want to update a part of the software or to fix some bugs without causing any damages in the master

How do the diagrams help you visualize the branch structure?
Diagrams show you the branches info, they show the commit that have been made in the different branches if it exist. 

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
 
What are the pros and cons of Git’s automatic merging vs. always doing merges
manually?  
